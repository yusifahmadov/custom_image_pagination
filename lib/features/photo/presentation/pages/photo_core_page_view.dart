import 'package:flutter/widgets.dart';

class PhotoCorePageView extends StatelessWidget {
  final Widget body;
  const PhotoCorePageView({super.key, required this.body});

  @override
  Widget build(BuildContext context) {
    return body;
  }
}
